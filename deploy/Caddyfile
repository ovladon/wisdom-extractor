# Point DNS A records for both names at the server IP; Caddy handles HTTPS itself.
wisdom.example.com {          # desktop portal
    reverse_proxy wisdom:8501
}
annotate.example.com {        # mobile swipe app — the link you share casually
    reverse_proxy mobile:8600
}
