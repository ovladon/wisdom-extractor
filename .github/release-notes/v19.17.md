New "Exact duplicate" judgment: flag word-for-word twins from overlapping collections.
Counts as the strongest same-idea signal for clustering and builds a human-verified
deduplication list, without widening the graded annotation scale.
