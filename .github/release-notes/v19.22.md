The analysis app is now live-first (auto-syncs the server database), supports
separate workspace databases with guarded merge into the corpus, gains an Admin
tab with all scientific metrics in one place, and ships an automated clustering-
threshold sensitivity sweep.
